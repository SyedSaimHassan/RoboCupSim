#ifndef DIMENSIONS_H
#define DIMENSIONS_H

namespace cfg {
struct Dimensions
{
    static float screenWidth;
    static float screenHeight;

    static const float fieldWidth;
    static const float fieldHeight;
    static const float fieldCentralCircleRadius;
    static const float fieldPlayingAreaWidth;
    static const float fieldPlayingAreaHeight;
    static const float fieldPenaltyAreaWidth;
    static const float fieldPenaltyAreaHeight;
    static const float fieldGoalWidth;
    static const float fieldGoalHeight;
    static const float fieldLineWidth;
    static const float outerFieldWidth;
    static const float outerFieldHeight;
};
}

#endif